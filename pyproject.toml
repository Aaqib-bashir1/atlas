[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas"
version = "0.1.0"
description = "Atlas data engineering pipeline"
requires-python = ">=3.9"
pyproject.toml